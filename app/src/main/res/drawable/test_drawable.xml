<?xml version="1.0" encoding="utf-8"?>
<rotate
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:drawable="@color/grey_light"
        android:fromDegrees="45"
        android:toDegrees="90"
        >

    <rotate
            android:drawable="@drawable/xishutai"
            android:fromDegrees="45"
            android:toDegrees="90"
            >
        <rotate
                android:drawable="@drawable/shuangrenchuang"
                android:fromDegrees="45"
                android:toDegrees="90"
                />
        <!-- 下层的直接覆盖掉上层的设置，只有最下层的drawable生效 -->
        <rotate
                android:drawable="@drawable/chuangtougui"
                android:fromDegrees="45"
                android:toDegrees="90"
                />
    </rotate>
</rotate>